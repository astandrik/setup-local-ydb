{
  "name": "setup-local-ydb",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action that starts local-ydb for CI jobs.",
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "build": "npm run compile && npm run bundle",
    "bundle": "ncc build lib/main.js -o dist/main --source-map --license licenses.txt && ncc build lib/post.js -o dist/post --source-map --license licenses.txt",
    "compile": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.typecheck.json"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^24.12.4",
    "@vercel/ncc": "^0.38.4",
    "typescript": "^5.7.3",
    "vitest": "^4.1.7"
  }
}
